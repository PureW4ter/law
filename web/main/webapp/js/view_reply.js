define(['ajaxhelper', 'utility'], function(ajaxHelper, util) {
    var Regist = {
        initialize: function() {
           timeBtn.initialize("i_getcode_btn");
        },
    };
    return Regist;
});